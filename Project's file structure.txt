noitu_game_api/
│   .env                    # Biến môi trường (chỉ local, không commit)
│   .env.example            # Mẫu biến môi trường
│   .gitignore              # Các file bị loại khỏi git
│   Dockerfile              # Cấu hình Docker cho môi trường phát triển
│   Dockerfile.prod         # Cấu hình Docker cho môi trường production
│   docker-compose.yml      # Cấu hình Docker Compose cho phát triển
│   docker-compose.prod.yml # Cấu hình Docker Compose cho production
│   main.py                 # Điểm khởi đầu ứng dụng
│   README.md               # Hướng dẫn sử dụng
│   requirements.txt        # Các thư viện cần thiết
│   __init__.py             # Khởi tạo package
│
├───app/                    # Thư mục chính của ứng dụng
│   │   config.py           # Cấu hình ứng dụng
│   │   __init__.py         # Khởi tạo package
│   │
│   ├───api/                # API endpoints và middleware
│   │   │   __init__.py     # Kết hợp tất cả routers
│   │   │   middleware.py   # Middleware xử lý request/response
│   │   │
│   │   └───routes/         # Thư mục chứa các routers tách biệt
│   │       │   __init__.py # Export tất cả routers
│   │       │   core.py     # Router chính (/ask endpoint)
│   │       │   game.py     # Router quản lý game
│   │       │   word.py     # Router quản lý từ
│   │       │   dictionary.py # Router quản lý từ điển  
│   │       └───system.py   # Router quản lý hệ thống
│   │
│   ├───models/             # Định nghĩa các models
│   │       schemas.py      # Pydantic schemas
│   │       __init__.py     # Khởi tạo package
│   │
│   ├───services/           # Các dịch vụ xử lý logic
│   │       ai_service.py   # Tương tác với Gemini API
│   │       game_service.py # Xử lý logic trò chơi
│   │       service_init.py # Khởi tạo các services
│   │       __init__.py     # Khởi tạo package
│   │
│   └───utils/              # Các tiện ích
│           constants.py    # Các hằng số
│           logging.py      # Cấu hình logging
│           validators.py   # Xác thực từ
│           word_evaluator.py # Đánh giá chất lượng từ
│           __init__.py     # Khởi tạo package
│
├───data/                   # Thư mục chứa dữ liệu
│       common_words.txt    # Danh sách từ phổ biến
│       quality_metrics.json # Số liệu về chất lượng từ
│       theme_words.json    # Từ được phân loại theo chủ đề
│       vietnamese_dictionary.txt # Từ điển tiếng Việt
│
├───scripts/                # Scripts tiện ích
│       create_dictionary.py # Tạo từ điển
│       __init__.py         # Khởi tạo package
│
└───tests/                  # Tests
        test_word_quality.py # Kiểm tra chất lượng từ
        __init__.py         # Khởi tạo package